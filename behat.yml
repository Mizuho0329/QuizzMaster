# behat.yml

default:
  suites:
    menu:
      contexts:
        - MenuContext
    nouvellePartie:
      contexts:
        - NouvellePartieContext
    option:
      contexts:
        - OptionContext
    partieUnJoueur:
      contexts:
        - PartieUnJoueurContext
    question:
      contexts:
        - QuestionContext
    resultat:
      contexts:
        - ResultatContext
